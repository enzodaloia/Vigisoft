{% extends 'base.html.twig' %}

{% block title %}Nouvelle Sévérité{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('app_back_severite_index') }}" class="btn btn-secondary mb-3">
            <i class="fa fa-arrow-left"></i>
        </a>
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0 text-center">Créer une nouvelle sévérité</h5>
            </div>
            <div class="card-body">
                {{ include('back/severite/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}