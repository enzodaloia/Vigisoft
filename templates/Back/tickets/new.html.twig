{% extends 'base.html.twig' %}

{% block title %}Nouvel Incident{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('app_back_tickets_index') }}" class="btn btn-secondary mb-3">
            <i class="fa fa-arrow-left"></i>
        </a>
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0 text-center">Créer un nouvel incident</h5>
            </div>
            <div class="card-body">
                {{ include('back/tickets/_form.html.twig', { 'button_label': 'Créer' }) }}
            </div>
        </div>
    </div>
{% endblock %}