{% extends 'base.html.twig' %}

{% block title %}Modifier le ticket{% endblock %}

{% block body %}
    <div class="container">
        <a href="{{ path('app_back_tickets_index') }}" class="btn btn-secondary mb-3">
            <i class="fa fa-arrow-left"></i>
        </a>

        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white">
                <h5 class="mb-0 text-center">Modifier le ticket</h5>
            </div>
            <div class="card-body">
                {{ include('back/tickets/_form.html.twig', { 'button_label': 'Mettre à jour' }) }}

                <hr>

                {{ include('back/tickets/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
