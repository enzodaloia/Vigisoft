{% extends 'base.html.twig' %}

{% block title %}Statut{% endblock %}

{% block body %}
    <h1>Statut</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ statut.titre }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ statut.createdAt ? statut.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_statut_index') }}">back to list</a>

    <a href="{{ path('app_back_statut_edit', {'id': statut.id}) }}">edit</a>

    {{ include('Back/statut/_delete_form.html.twig') }}
{% endblock %}
